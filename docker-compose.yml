version: '3.0'

services:
  bin:
    build:
      context: .
      dockerfile: Dockerfile
    command: "go test -v"
    ports:
      - "22:22"
    volumes:
      - .:/go/src/app
    env_file: .secrets
